# marijanapav.com — Design System Reference

## 1. Visual Theme & Atmosphere

Quiet intelligence — a portfolio where colour does nothing and type does everything. The Lab colour space anchors a near-black-and-white palette with surgical precision. Archivo (a display grotesque) handles headings, IBM Plex Mono brings monospace credibility to labels and micro-text, and Inter provides readable comfort for body copy. The triple typeface pairing is disciplined: each face has a strict role and never crosses boundaries.

---

## 2. Color Palette & Roles

- **background**: lab(100 0 0) — mathematically perfect white
- **text**: rgb(0, 0, 0) — absolute black
- **surfaceTint**: lab(92.38 −1.49 0) / 0.9 — barely-there warm white overlay
- **secondary**: lab(30.4 0 0) — dark gray for secondary text

*Notably absent:* Colour

---

## 3. Typography Rules

**Typeface(s):** Archivo (headings), IBM Plex Mono (labels/code), Inter (body)

**Tech stack fonts hint:** Next.js, Tailwind CSS

- **h1**: 16px Archivo 400 (editorial small headings)
- **h2**: 12px IBM Plex Mono 600 uppercase, 2.4px letter-spacing (section labels)
- **body**: 16px/24px Inter 400
- **p**: 14px/24px Inter 400

**Philosophy:** Each size level reinforces hierarchy without relying on colour.

---

## 4. Component Stylings

**Border Radius:** None — brutalist sharp edges throughout

**Shadows:** None — depth through negative space only

**Gradients:** None

Components lean on spacing and typography for distinction rather than decorative chrome. Buttons likely use filled + ghost variants in the primary colour.

---

## 5. Layout Principles

Built on **Next.js + Tailwind CSS**. Layouts follow a content-first grid — wide gutters, deliberate whitespace rhythm. Section breaks use background-colour contrast rather than divider lines.

---

## 6. Depth & Elevation

Elevation is communicated through colour contrast and spacing alone — no box-shadow stack exists. This creates a completely flat, layered paper feel.

---

## 7. Do's and Don'ts

**Do:**
- Use the exact colour roles as defined — each serves a specific semantic purpose
- Respect the typeface role boundaries (each font has one job)
- Preserve generous whitespace — crowding breaks the design language
- Keep interactions restrained: hover opacity or colour-shift, not complex transforms

**Don't:**
- Introduce colours outside the defined palette
- Mix typefaces beyond the established pairing
- Add drop shadows where the design is intentionally flat
- Use gradients unless the site already employs them
- Add Colour
- Add shadows

---

## 8. Responsive Behavior

Next.js/Tailwind; single-column mobile preserves austere character

On mobile, the core design language holds — the reduction is spatial (tighter gutters, stacked columns) not stylistic. Colour, typography, and component styles remain unchanged.

---

## 9. Agent Prompt Guide

> Use this as a one-paragraph brief when prompting an AI to build in this design language:

**"Portfolio with strict type hierarchy: Archivo for editorial headings (small-sized), IBM Plex Mono for uppercase label captions (+2.4px spacing), Inter for body; black-on-white only; zero shadows, zero radius, zero colour"**

---

*Generated by Sparkbites — extracted from live CSS analysis*
